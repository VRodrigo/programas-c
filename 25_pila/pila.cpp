#include <stdio.h>
#include <stdlib.h>

int main(int argc, const char **argv){

    int *a = NULL;
    int filas, col;

    printf("Numero de filas-columnas: ");
    scanf(" %i - %i", filas, col);

    *a = realloc(*sizeof(int))
    return EXIT_SUCCESS;

}
