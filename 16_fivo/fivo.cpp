#include <stdio.h>
#include <stdlib.h>
#define N 15;

void imprime(int A[n]) {
    for(int i=0; i<N; i++)
        printf("%i", A[i])
}

int main(){

    int a[N];

    a[0] = a[1] = 1

    for(int i=0; i<20; i++)
        a[i] = a[i-2] + a[i-1];

    return EXIT_SUCCESS;

}
